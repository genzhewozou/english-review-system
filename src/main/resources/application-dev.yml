# Development Profile Configuration

# H2 Database for Development (easier setup)
spring:
  datasource:
    url: jdbc:mysql://sh-cdb-l89atvre.sql.tencentcdb.com:21231/english_learning_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: woaini,1996
    driver-class-name: com.mysql.cj.jdbc.Driver
#  datasource:
#    url: jdbc:h2:mem:english_learning_dev;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
  
  # H2 Console Configuration
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
  
  # JPA Configuration for Development
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Development File Storage (smaller limits for testing)
file:
  storage:
    base-path: ./uploads/dev
    max-file-size: 50MB

# Development Logging
logging:
  level:
    org.example.docvideoplay: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    root: INFO