我是java开发者，我遇到一个问题。我有一个定时job，这个job的功能是：查询A表的数据，查询出状态是0的数据，然后进行一系列校验，随后会调用支付宝接口进行发钱，调用支付宝成功后，会根据支付宝返回的状态再去更新A表的状态字段。但是在这个过程中，支付宝调用成功后，我在后续更新的时候发生了错误，导致事务回滚，这个状态没有变，那么在第二次继续执行这个job的时候，由于状态没有成功变更，就会重新查到这个数据，导致重复调用支付宝发放。我该如何做，可以避免在失败后继续查到那个已经发放的数据？有一个前提，我无法查询支付宝我是否已经调用成功。

我是java开发者，我遇到一个问题。我在开发word时，使用XWPFRun替换文字时，如果所需要替换文字过多，是否会导致换行等问题，我所需要替换的内容，下面有黑色划线，如果替换内容过多，黑色划线是否也会跟着换行？我该怎么做能保证样式正确。

